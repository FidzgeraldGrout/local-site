import Head from 'next/head'

export default function Authorization({ isFirstMount }) {
  return (
    <div>
        <Head>
          <title>Авторизация</title>
          <link rel="icon" href="/favicon.ico" />
        </Head>
    </div>
  )
}
